# AstraResume (MVP)

AstraResume — Azure + Semantic Kernel powered resume analyzer (Streamlit MVP).

## Quickstart (local)
1. Copy `.env.example` to `.env` and fill Azure values.
2. Create a virtual env and install:
   ```bash
   pip install -r requirements.txt

